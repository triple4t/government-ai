# API base URL (no trailing slash). Used at build time for production.
# Development: leave unset to use http://localhost:8000
# Production: set to your backend URL, e.g.:
# VITE_API_URL=https://api.yourdomain.com
